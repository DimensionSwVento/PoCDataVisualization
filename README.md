# PoCDataVisualization

<h2>1. Overview</h2>
<p> 
This project is a <strong>proof of concept (PoC)</strong> looking to create a data visualization map using <strong>Python, pandas and leaflet</strong> in order to review the quality of the information generated once  the original datasets provided by other teams are transformed to the <strong>H3 format on a parquet file</strong> 
</p>
<p>
The PoC focuses on the ability to generate <strong>multilayered maps as html offline visualization files</strong> using the different columns of the resulting dataset regardless of the amount of data in order to share them with the teams providing the original information for it's review
</p>

<hr>

<h2>2. Objectives</h2>
<ul>
  <li>create a visualization map that can be shared and reviewed by the providing team, regardless of their technical knowledge on GIS tools.</li>
  <li>create a tool that can be used to generate maps using open source tools</li>
  <li>create a tool that can be integrated on the workflow of the team without the use of a registered license.</li>
</ul>

<hr>

<h2>3. Technologies Used</h2>
<table>
  <thead>
    <tr>
      <th>Technology</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Python 3</td>
      <td>Core programming language</td>
    </tr>
    <tr>
      <td>leaflet</td>
      <td>map graphication tool</td>
    </tr>
    <tr>
      <td>pandas</td>
      <td>Data processing tool</td>
    </tr>
    <tr>
      <td>h3</td>
      <td>Hexagon geography based tool </td>
    </tr>
    <tr>
      <td>Virtual Environment (venv)</td>
      <td>Dependency isolation</td>
    </tr>
  </tbody>
</table>

<hr>

<h2>4. Project Structure</h2>
<pre>
EjemploGIS/
│
├── .venv/                  # Python virtual environment
├── DataVisualization.py    # first version of the python script
├── DataVisualization_v2.py # second version of the Python script
├── README.md               # Project documentation
└── requirements.txt        # Project dependencies
</pre>

<hr>

<h2>5. Installation and Setup</h2>

<h3>5.1 Create and activate a virtual environment</h3>

<pre>
python -m venv .venv
</pre>

<p>Activate the environment:</p>

<p><strong>Windows (CMD):</strong></p>
<pre>
.venv\Scripts\activate
</pre>

<p><strong>Windows (PowerShell):</strong></p>
<pre>
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.venv\Scripts\Activate.ps1
</pre>

<hr>

<h3>5.2 Install dependencies</h3>

<pre>
pip install pandas leaflet h3 fastparquet
</pre>

<p>Alternatively, using <code>requirements.txt</code>:</p>

<pre>
pip install -r requirements.txt
</pre>

<hr>

<h2>6. Data Source</h2>
<p>
This proof of concept uses <strong>randomly generated information</strong>, which was generated by creating a h3 table on ArcGIS, and subsequently adding random numbers to the table
</p>

<hr>

<h2>7. known issues</h2>

at the moment both versions of the code are unable to complete it's execution, it is currently known this due to the implementation of the pandas library, which needs to be optimized

<img width="1200" height="362" alt="execution" src="https://github.com/user-attachments/assets/84bcb343-8614-4030-84cd-f0192dd33d92" />
